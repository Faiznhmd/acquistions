pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build the app'
            }
        }
        stage('test') {
            steps {
                echo 'Running the Test'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying the app'
            }
        }
    }
}
